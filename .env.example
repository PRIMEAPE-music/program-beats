# Set one or both AI provider keys
# The app will use whichever is available (Claude preferred if both set)
# You can switch between them in the UI via the dropdown in the chat panel

ANTHROPIC_API_KEY=your-claude-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here
